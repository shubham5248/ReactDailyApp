import React,{Component} from "react";

export default class ArrayMapping extends Component{

    render(){
        const Array=[1,2,3,4];
        return(
        <>
        const NewArray=Array.map((element)=>element)); // should return [1,2,3,4]
        </>
        )
        
        
    }
}